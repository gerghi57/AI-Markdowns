- name: Debug repository
  run: |
    echo "Repository: ${{ github.repository }}"
    echo "Upstream: ${{ vars.UPSTREAM_REPO }}"
    echo "Branch: ${{ vars.BRANCH }}"
    git remote -v
    git branch -a
    git status
